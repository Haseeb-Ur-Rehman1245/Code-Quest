import { CourseLessons } from "./types";

export const cppLessons: CourseLessons = {
  easy: [
    { id: "cpp-1", title: "Introduction to C++", content: "C++ is a powerful, high-performance language created by Bjarne Stroustrup in 1979 as an extension of C. It supports procedural, object-oriented, and generic programming. C++ is used in game engines, operating systems, browsers, embedded systems, and competitive programming. It compiles directly to machine code for maximum performance.", codeExamples: [{ language: "cpp", code: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // Variables\n    int age = 25;\n    double gpa = 3.8;\n    string name = "Ali";\n    bool isStudent = true;\n\n    // Output\n    cout << "Name: " << name << endl;\n    cout << "Age: " << age << endl;\n    cout << "GPA: " << gpa << endl;\n\n    // Input\n    cout << "Enter your name: ";\n    cin >> name;\n    cout << "Hello, " << name << "!" << endl;\n\n    return 0;\n}', explanation: "Basic C++ program with variables, input, and output." }], exercises: [{ question: "Write a program that takes two numbers and prints their sum, difference, product, and quotient.", solution: '#include <iostream>\nusing namespace std;\nint main() {\n    double a, b;\n    cout << "Enter two numbers: ";\n    cin >> a >> b;\n    cout << "Sum: " << a + b << endl;\n    cout << "Difference: " << a - b << endl;\n    cout << "Product: " << a * b << endl;\n    if (b != 0) cout << "Quotient: " << a / b << endl;\n    return 0;\n}', difficulty: "beginner" }], externalLinks: [{ title: "C++ Reference", url: "https://cppreference.com/" }, { title: "W3Schools C++", url: "https://www.w3schools.com/cpp/" }, { title: "GeeksforGeeks C++", url: "https://www.geeksforgeeks.org/c-plus-plus/" }], estimatedMinutes: 20 },
    { id: "cpp-2", title: "Data Types & Variables", content: "C++ has fundamental types: int (integer), double/float (decimal), char (character), bool (boolean), string (text). Use const for constants. sizeof() returns type size in bytes. Type casting: static_cast<type>(value). auto keyword lets compiler infer types. C++ is strongly typed — every variable must have a declared type.", codeExamples: [{ language: "cpp", code: '#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    // Fundamental types\n    int count = 42;\n    double price = 19.99;\n    char grade = \'A\';\n    bool passed = true;\n    string name = "Ali";\n\n    // Constants\n    const double PI = 3.14159;\n    const int MAX_SIZE = 100;\n\n    // Auto type inference\n    auto x = 10;        // int\n    auto y = 3.14;      // double\n    auto s = "hello"s;   // string\n\n    // Type casting\n    int a = 7, b = 2;\n    double result = static_cast<double>(a) / b;  // 3.5\n    cout << result << endl;\n\n    return 0;\n}', explanation: "C++ data types, constants, and type casting." }], exercises: [{ question: "Create variables for a student record (name, age, GPA, enrollment status) and print them.", solution: 'int main() {\n    string name = "Ahmed";\n    int age = 20;\n    double gpa = 3.7;\n    bool enrolled = true;\n    cout << name << ", " << age << ", GPA: " << gpa << ", Enrolled: " << boolalpha << enrolled;\n    return 0;\n}', difficulty: "beginner" }], externalLinks: [{ title: "C++ Data Types", url: "https://www.w3schools.com/cpp/cpp_data_types.asp" }], estimatedMinutes: 15 },
    { id: "cpp-3", title: "Control Flow", content: "C++ uses if/else if/else for conditions, switch for value matching. Comparison operators: ==, !=, <, >, <=, >=. Logical operators: && (AND), || (OR), ! (NOT). The ternary operator (condition ? a : b) provides shorthand conditions. Switch requires break to prevent fall-through.", codeExamples: [{ language: "cpp", code: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int score;\n    cout << "Enter score: ";\n    cin >> score;\n\n    // if/else\n    if (score >= 90) cout << "Grade: A" << endl;\n    else if (score >= 80) cout << "Grade: B" << endl;\n    else if (score >= 70) cout << "Grade: C" << endl;\n    else cout << "Grade: F" << endl;\n\n    // Ternary\n    string status = (score >= 60) ? "Pass" : "Fail";\n    cout << status << endl;\n\n    // Switch\n    int day = 3;\n    switch (day) {\n        case 1: cout << "Monday"; break;\n        case 2: cout << "Tuesday"; break;\n        case 3: cout << "Wednesday"; break;\n        default: cout << "Other day";\n    }\n    return 0;\n}', explanation: "Conditional statements in C++." }], exercises: [{ question: "Write a program that determines if a year is a leap year.", solution: 'bool isLeapYear(int year) {\n    return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);\n}', difficulty: "beginner" }], externalLinks: [{ title: "C++ Control Flow", url: "https://www.w3schools.com/cpp/cpp_conditions.asp" }], estimatedMinutes: 20 },
    { id: "cpp-4", title: "Loops", content: "C++ supports for, while, and do-while loops. Range-based for loops iterate over containers elegantly. Use break to exit early, continue to skip iterations. Nested loops handle 2D structures like matrices. The for loop has initialization, condition, and increment sections.", codeExamples: [{ language: "cpp", code: '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    // Traditional for loop\n    for (int i = 0; i < 5; i++) {\n        cout << i << " ";\n    }\n    cout << endl;\n\n    // Range-based for loop\n    vector<string> fruits = {"apple", "banana", "cherry"};\n    for (const auto& fruit : fruits) {\n        cout << fruit << endl;\n    }\n\n    // While loop\n    int n = 1;\n    while (n <= 10) {\n        cout << n++ << " ";\n    }\n\n    // Multiplication table\n    for (int i = 1; i <= 5; i++) {\n        for (int j = 1; j <= 10; j++) {\n            cout << i * j << "\\t";\n        }\n        cout << endl;\n    }\n    return 0;\n}', explanation: "Loop types including range-based for." }], exercises: [{ question: "Print a pattern of stars forming a right triangle.", solution: 'for (int i = 1; i <= 5; i++) {\n    for (int j = 0; j < i; j++)\n        cout << "* ";\n    cout << endl;\n}\n// *\n// * *\n// * * *\n// * * * *\n// * * * * *', difficulty: "beginner" }], externalLinks: [{ title: "C++ Loops", url: "https://www.w3schools.com/cpp/cpp_for_loop.asp" }], estimatedMinutes: 20 },
    { id: "cpp-5", title: "Functions", content: "Functions encapsulate reusable logic. Declare return type, name, and parameters. Use pass by value (copy), pass by reference (&), or pass by const reference (const &). Function overloading allows multiple functions with the same name but different parameters. Default parameters provide fallback values.", codeExamples: [{ language: "cpp", code: '// Pass by value vs reference\nvoid doubleValue(int x) { x *= 2; }          // copy - original unchanged\nvoid doubleRef(int& x) { x *= 2; }           // reference - modifies original\n\n// Function overloading\nint add(int a, int b) { return a + b; }\ndouble add(double a, double b) { return a + b; }\nstring add(string a, string b) { return a + b; }\n\n// Default parameters\nvoid greet(string name, string greeting = "Hello") {\n    cout << greeting << ", " << name << "!" << endl;\n}\n\n// Template function (generic)\ntemplate <typename T>\nT getMax(T a, T b) {\n    return (a > b) ? a : b;\n}', explanation: "Functions with references, overloading, defaults, and templates." }], exercises: [{ question: "Write a function that swaps two integers using pass by reference.", solution: 'void swap(int& a, int& b) {\n    int temp = a;\n    a = b;\n    b = temp;\n}\n// Usage:\nint x = 5, y = 10;\nswap(x, y);\ncout << x << " " << y;  // 10 5', difficulty: "beginner" }], externalLinks: [{ title: "C++ Functions", url: "https://www.w3schools.com/cpp/cpp_functions.asp" }], estimatedMinutes: 25 },
    { id: "cpp-6", title: "Arrays & Vectors", content: "Arrays are fixed-size containers. Vectors (std::vector) are dynamic arrays from the STL that can grow/shrink. Prefer vectors over raw arrays for safety and flexibility. Methods: push_back, pop_back, size, empty, at, front, back. Use iterators or range-based for loops to traverse.", codeExamples: [{ language: "cpp", code: '#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    // Vector\n    vector<int> nums = {5, 2, 8, 1, 9, 3};\n    nums.push_back(7);       // add to end\n    nums.pop_back();         // remove from end\n\n    // Sort\n    sort(nums.begin(), nums.end());\n\n    // Find\n    auto it = find(nums.begin(), nums.end(), 8);\n    if (it != nums.end()) {\n        cout << "Found 8 at index " << (it - nums.begin()) << endl;\n    }\n\n    // Range-based loop\n    for (int n : nums) cout << n << " ";\n\n    // 2D vector\n    vector<vector<int>> matrix = {\n        {1, 2, 3},\n        {4, 5, 6},\n        {7, 8, 9}\n    };\n    return 0;\n}', explanation: "Vectors with sorting, searching, and iteration." }], exercises: [{ question: "Write a function that finds the second largest element in a vector.", solution: 'int secondLargest(vector<int> nums) {\n    sort(nums.rbegin(), nums.rend());\n    for (int i = 1; i < nums.size(); i++) {\n        if (nums[i] != nums[0]) return nums[i];\n    }\n    return -1; // all elements same\n}', difficulty: "beginner" }], externalLinks: [{ title: "C++ Vectors", url: "https://cplusplus.com/reference/vector/vector/" }], estimatedMinutes: 25 },
    { id: "cpp-7", title: "Strings in C++", content: "C++ has two string types: C-strings (char arrays) and std::string. Use std::string for safety and convenience. Methods: length, substr, find, replace, append, compare. String concatenation with +. Convert with stoi, stod, to_string. getline reads entire lines including spaces.", codeExamples: [{ language: "cpp", code: '#include <string>\nusing namespace std;\n\nint main() {\n    string text = "Hello, World!";\n    \n    cout << text.length() << endl;          // 13\n    cout << text.substr(0, 5) << endl;       // "Hello"\n    cout << text.find("World") << endl;      // 7\n    \n    text.replace(7, 5, "C++");               // "Hello, C++!"\n    \n    // Conversion\n    int num = stoi("42");\n    string numStr = to_string(42);\n    \n    // Read full line\n    string fullName;\n    cout << "Enter full name: ";\n    getline(cin, fullName);\n    \n    // Iterate characters\n    for (char c : text) {\n        cout << c << " ";\n    }\n    return 0;\n}', explanation: "std::string operations and conversions." }], exercises: [{ question: "Write a function to check if a string is a palindrome.", solution: 'bool isPalindrome(string s) {\n    int left = 0, right = s.length() - 1;\n    while (left < right) {\n        if (tolower(s[left]) != tolower(s[right])) return false;\n        left++; right--;\n    }\n    return true;\n}', difficulty: "beginner" }], externalLinks: [{ title: "C++ Strings", url: "https://www.w3schools.com/cpp/cpp_strings.asp" }], estimatedMinutes: 20 },
    { id: "cpp-8", title: "Pointers Basics", content: "Pointers store memory addresses. Declare with * (int* ptr). Get address with & (address-of operator). Dereference with * to access the value. nullptr represents a null pointer. Pointers enable dynamic memory allocation, passing large data efficiently, and implementing data structures.", codeExamples: [{ language: "cpp", code: 'int main() {\n    int x = 42;\n    int* ptr = &x;       // ptr holds address of x\n\n    cout << "Value: " << x << endl;        // 42\n    cout << "Address: " << &x << endl;      // 0x...\n    cout << "Pointer: " << ptr << endl;     // same address\n    cout << "Deref: " << *ptr << endl;      // 42\n\n    *ptr = 100;          // modify x through pointer\n    cout << x << endl;   // 100\n\n    // Pointer arithmetic (arrays)\n    int arr[] = {10, 20, 30, 40};\n    int* p = arr;        // points to first element\n    cout << *(p + 2) << endl;  // 30\n\n    // Dynamic memory\n    int* dynamic = new int(42);\n    cout << *dynamic << endl;  // 42\n    delete dynamic;            // free memory\n\n    return 0;\n}', explanation: "Pointer basics: declaration, dereferencing, and dynamic memory." }], exercises: [{ question: "Write a function that swaps two values using pointers.", solution: 'void swap(int* a, int* b) {\n    int temp = *a;\n    *a = *b;\n    *b = temp;\n}\n// Usage:\nint x = 5, y = 10;\nswap(&x, &y);', difficulty: "beginner" }], externalLinks: [{ title: "C++ Pointers", url: "https://www.w3schools.com/cpp/cpp_pointers.asp" }], estimatedMinutes: 30 },
  ],
  intermediate: [
    { id: "cpp-9", title: "Object-Oriented Programming", content: "C++ classes encapsulate data and methods. Access modifiers: public, private, protected. Constructors initialize objects; destructors clean up. Use this-> for member access. Operator overloading customizes operators for classes. Copy constructors handle object copying.", codeExamples: [{ language: "cpp", code: 'class Student {\nprivate:\n    string name;\n    vector<int> grades;\n\npublic:\n    // Constructor\n    Student(string n) : name(n) {}\n\n    // Methods\n    void addGrade(int g) { grades.push_back(g); }\n\n    double average() const {\n        if (grades.empty()) return 0;\n        int sum = 0;\n        for (int g : grades) sum += g;\n        return static_cast<double>(sum) / grades.size();\n    }\n\n    // Operator overloading\n    friend ostream& operator<<(ostream& os, const Student& s) {\n        os << s.name << " (Avg: " << s.average() << ")";\n        return os;\n    }\n};', explanation: "C++ class with constructor, methods, and operator overloading." }], exercises: [{ question: "Create a Rectangle class with area, perimeter methods and comparison operators.", solution: 'class Rectangle {\n    double width, height;\npublic:\n    Rectangle(double w, double h) : width(w), height(h) {}\n    double area() const { return width * height; }\n    double perimeter() const { return 2 * (width + height); }\n    bool operator>(const Rectangle& other) const {\n        return area() > other.area();\n    }\n};', difficulty: "intermediate" }], externalLinks: [{ title: "C++ Classes", url: "https://www.w3schools.com/cpp/cpp_classes.asp" }], estimatedMinutes: 30 },
    { id: "cpp-10", title: "Inheritance & Polymorphism", content: "Inheritance creates class hierarchies. A derived class inherits from a base class. Virtual functions enable polymorphism — the correct method is called based on the actual object type at runtime. Pure virtual functions (= 0) create abstract classes. Use override keyword to mark overridden methods.", codeExamples: [{ language: "cpp", code: 'class Shape {\npublic:\n    virtual double area() const = 0;        // pure virtual\n    virtual string name() const = 0;\n    virtual ~Shape() = default;              // virtual destructor\n\n    void print() const {\n        cout << name() << ": area = " << area() << endl;\n    }\n};\n\nclass Circle : public Shape {\n    double radius;\npublic:\n    Circle(double r) : radius(r) {}\n    double area() const override { return 3.14159 * radius * radius; }\n    string name() const override { return "Circle"; }\n};\n\nclass Rectangle : public Shape {\n    double w, h;\npublic:\n    Rectangle(double w, double h) : w(w), h(h) {}\n    double area() const override { return w * h; }\n    string name() const override { return "Rectangle"; }\n};\n\n// Polymorphism in action\nvoid printShapes(const vector<Shape*>& shapes) {\n    for (const auto* s : shapes) s->print();\n}', explanation: "Polymorphism with virtual functions and abstract classes." }], exercises: [{ question: "Create an Animal hierarchy with speak() virtual method.", solution: 'class Animal {\npublic:\n    virtual string speak() const = 0;\n    virtual ~Animal() = default;\n};\nclass Dog : public Animal {\npublic:\n    string speak() const override { return "Woof!"; }\n};\nclass Cat : public Animal {\npublic:\n    string speak() const override { return "Meow!"; }\n};', difficulty: "intermediate" }], externalLinks: [{ title: "C++ Inheritance", url: "https://www.w3schools.com/cpp/cpp_inheritance.asp" }], estimatedMinutes: 30 },
    { id: "cpp-11", title: "STL Containers & Algorithms", content: "The Standard Template Library (STL) provides containers (vector, map, set, queue, stack), iterators, and algorithms (sort, find, count, transform, accumulate). Maps store key-value pairs. Sets store unique elements. Unordered variants use hash tables for O(1) access.", codeExamples: [{ language: "cpp", code: '#include <map>\n#include <set>\n#include <algorithm>\n#include <numeric>\n\nint main() {\n    // Map\n    map<string, int> scores;\n    scores["Ali"] = 95;\n    scores["Sara"] = 88;\n    for (auto& [name, score] : scores)\n        cout << name << ": " << score << endl;\n\n    // Set\n    set<int> unique = {3, 1, 4, 1, 5, 9}; // {1, 3, 4, 5, 9}\n\n    // Algorithms\n    vector<int> nums = {5, 2, 8, 1, 9};\n    sort(nums.begin(), nums.end());\n    \n    int sum = accumulate(nums.begin(), nums.end(), 0);\n    int count = count_if(nums.begin(), nums.end(), [](int n){ return n > 5; });\n    \n    // Transform\n    vector<int> doubled;\n    transform(nums.begin(), nums.end(), back_inserter(doubled),\n              [](int n){ return n * 2; });\n    return 0;\n}', explanation: "STL containers and algorithms with lambdas." }], exercises: [{ question: "Use a map to count word frequency in a sentence.", solution: 'map<string, int> wordCount(const string& text) {\n    map<string, int> freq;\n    istringstream iss(text);\n    string word;\n    while (iss >> word) freq[word]++;\n    return freq;\n}', difficulty: "intermediate" }], externalLinks: [{ title: "C++ STL", url: "https://cplusplus.com/reference/stl/" }], estimatedMinutes: 30 },
    { id: "cpp-12", title: "Smart Pointers & Memory Management", content: "Smart pointers automatically manage memory. unique_ptr has exclusive ownership, shared_ptr has shared ownership with reference counting, weak_ptr breaks circular references. Use make_unique and make_shared for creation. Never use raw new/delete in modern C++ — always prefer smart pointers.", codeExamples: [{ language: "cpp", code: '#include <memory>\n\nclass Widget {\npublic:\n    Widget(string n) : name(n) { cout << "Created " << name << endl; }\n    ~Widget() { cout << "Destroyed " << name << endl; }\n    string name;\n};\n\nint main() {\n    // unique_ptr - exclusive ownership\n    auto w1 = make_unique<Widget>("A");\n    // auto w2 = w1; // ERROR! Can\'t copy\n    auto w2 = move(w1);  // Transfer ownership\n\n    // shared_ptr - shared ownership\n    auto s1 = make_shared<Widget>("B");\n    auto s2 = s1;  // Both own it\n    cout << "Use count: " << s1.use_count() << endl;  // 2\n\n    // Vector of unique_ptr\n    vector<unique_ptr<Widget>> widgets;\n    widgets.push_back(make_unique<Widget>("C"));\n    widgets.push_back(make_unique<Widget>("D"));\n    // Automatically cleaned up when vector goes out of scope\n    return 0;\n}', explanation: "Smart pointers for automatic memory management." }], exercises: [{ question: "Refactor raw pointer code to use smart pointers.", solution: '// Before:\n// Widget* w = new Widget("test");\n// delete w;\n\n// After:\nauto w = make_unique<Widget>("test");\n// No delete needed!', difficulty: "intermediate" }], externalLinks: [{ title: "C++ Smart Pointers", url: "https://en.cppreference.com/w/cpp/memory" }], estimatedMinutes: 30 },
  ],
  hard: [
    { id: "cpp-13", title: "Templates & Generic Programming", content: "Templates enable writing code that works with any type. Function templates and class templates use template<typename T> syntax. Template specialization customizes behavior for specific types. Variadic templates accept any number of arguments. SFINAE and concepts (C++20) constrain template parameters.", codeExamples: [{ language: "cpp", code: '// Function template\ntemplate <typename T>\nT maximum(T a, T b) {\n    return (a > b) ? a : b;\n}\n\n// Class template\ntemplate <typename T>\nclass Stack {\n    vector<T> data;\npublic:\n    void push(const T& item) { data.push_back(item); }\n    T pop() {\n        T top = data.back();\n        data.pop_back();\n        return top;\n    }\n    bool empty() const { return data.empty(); }\n    size_t size() const { return data.size(); }\n};\n\n// C++20 Concepts\ntemplate <typename T>\nconcept Numeric = is_arithmetic_v<T>;\n\ntemplate <Numeric T>\nT add(T a, T b) { return a + b; }\n\n// Usage\nStack<int> intStack;\nStack<string> strStack;\nauto result = maximum(10, 20);    // int\nauto result2 = maximum(3.14, 2.71); // double', explanation: "Templates for generic type-safe programming." }], exercises: [{ question: "Create a generic Pair<T, U> class with first and second members.", solution: 'template <typename T, typename U>\nclass Pair {\npublic:\n    T first;\n    U second;\n    Pair(T f, U s) : first(f), second(s) {}\n    void print() const {\n        cout << "(" << first << ", " << second << ")";\n    }\n};', difficulty: "advanced" }], externalLinks: [{ title: "C++ Templates", url: "https://en.cppreference.com/w/cpp/language/templates" }], estimatedMinutes: 35 },
    { id: "cpp-14", title: "Move Semantics & Rvalue References", content: "Move semantics (C++11) efficiently transfer resources instead of copying. Rvalue references (&&) bind to temporary values. std::move casts to rvalue reference. Move constructors and move assignment operators steal resources from temporaries. The Rule of Five: if you define any of destructor, copy/move constructor, copy/move assignment, define all five.", codeExamples: [{ language: "cpp", code: 'class Buffer {\n    int* data;\n    size_t size;\npublic:\n    // Constructor\n    Buffer(size_t n) : data(new int[n]), size(n) {\n        cout << "Constructed " << size << endl;\n    }\n\n    // Move constructor\n    Buffer(Buffer&& other) noexcept\n        : data(other.data), size(other.size) {\n        other.data = nullptr;\n        other.size = 0;\n        cout << "Moved" << endl;\n    }\n\n    // Move assignment\n    Buffer& operator=(Buffer&& other) noexcept {\n        if (this != &other) {\n            delete[] data;\n            data = other.data; size = other.size;\n            other.data = nullptr; other.size = 0;\n        }\n        return *this;\n    }\n\n    ~Buffer() { delete[] data; }\n};\n\nBuffer createBuffer() {\n    return Buffer(1000);  // Move, not copy!\n}', explanation: "Move semantics for efficient resource transfer." }], exercises: [{ question: "Add move semantics to a DynamicArray class.", solution: 'class DynamicArray {\n    int* arr; size_t sz;\npublic:\n    DynamicArray(size_t n) : arr(new int[n]{0}), sz(n) {}\n    DynamicArray(DynamicArray&& o) noexcept : arr(o.arr), sz(o.sz) {\n        o.arr = nullptr; o.sz = 0;\n    }\n    ~DynamicArray() { delete[] arr; }\n};', difficulty: "advanced" }], externalLinks: [{ title: "Move Semantics", url: "https://en.cppreference.com/w/cpp/language/move_constructor" }], estimatedMinutes: 35 },
    { id: "cpp-15", title: "Multithreading", content: "C++11 introduced std::thread for multithreading. Use mutex and lock_guard for thread safety. atomic types for lock-free operations. async and future for parallel tasks. Condition variables for thread synchronization. Avoid data races by protecting shared data.", codeExamples: [{ language: "cpp", code: '#include <thread>\n#include <mutex>\n#include <future>\n\nmutex mtx;\nint counter = 0;\n\nvoid increment(int times) {\n    for (int i = 0; i < times; i++) {\n        lock_guard<mutex> lock(mtx);\n        counter++;\n    }\n}\n\nint main() {\n    // Threads\n    thread t1(increment, 100000);\n    thread t2(increment, 100000);\n    t1.join();\n    t2.join();\n    cout << counter << endl;  // 200000\n\n    // Async\n    auto future1 = async(launch::async, []() {\n        this_thread::sleep_for(chrono::seconds(1));\n        return 42;\n    });\n    cout << "Result: " << future1.get() << endl;\n    return 0;\n}', explanation: "Multithreading with mutex protection and async." }], exercises: [{ question: "Create a thread-safe counter class.", solution: 'class SafeCounter {\n    int count = 0;\n    mutable mutex mtx;\npublic:\n    void increment() {\n        lock_guard<mutex> lock(mtx);\n        count++;\n    }\n    int get() const {\n        lock_guard<mutex> lock(mtx);\n        return count;\n    }\n};', difficulty: "advanced" }], externalLinks: [{ title: "C++ Threads", url: "https://en.cppreference.com/w/cpp/thread" }], estimatedMinutes: 35 },
    { id: "cpp-16", title: "Data Structures Implementation", content: "Implement fundamental data structures in C++: linked lists, binary trees, hash maps, and graphs. Understanding these is crucial for competitive programming and technical interviews. C++ is ideal for DSA due to its performance and STL support.", codeExamples: [{ language: "cpp", code: '// Linked List\ntemplate <typename T>\nstruct Node {\n    T data;\n    Node* next;\n    Node(T val) : data(val), next(nullptr) {}\n};\n\ntemplate <typename T>\nclass LinkedList {\n    Node<T>* head = nullptr;\npublic:\n    void push_front(T val) {\n        auto* node = new Node<T>(val);\n        node->next = head;\n        head = node;\n    }\n\n    void print() const {\n        for (auto* curr = head; curr; curr = curr->next)\n            cout << curr->data << " -> ";\n        cout << "null" << endl;\n    }\n\n    ~LinkedList() {\n        while (head) {\n            auto* temp = head;\n            head = head->next;\n            delete temp;\n        }\n    }\n};', explanation: "Linked list implementation with templates." }], exercises: [{ question: "Implement a binary search tree with insert and search.", solution: 'struct BSTNode {\n    int data;\n    BSTNode *left = nullptr, *right = nullptr;\n    BSTNode(int val) : data(val) {}\n};\n\nBSTNode* insert(BSTNode* root, int val) {\n    if (!root) return new BSTNode(val);\n    if (val < root->data) root->left = insert(root->left, val);\n    else root->right = insert(root->right, val);\n    return root;\n}\n\nbool search(BSTNode* root, int val) {\n    if (!root) return false;\n    if (val == root->data) return true;\n    return val < root->data ? search(root->left, val) : search(root->right, val);\n}', difficulty: "advanced" }], externalLinks: [{ title: "LeetCode C++", url: "https://leetcode.com/problemset/" }, { title: "GeeksforGeeks DSA", url: "https://www.geeksforgeeks.org/data-structures/" }], estimatedMinutes: 40 },
  ],
};
