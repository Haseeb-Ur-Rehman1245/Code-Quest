import { CourseLessons } from "./types";

export const reactLessons: CourseLessons = {
  easy: [
    { id: "react-1", title: "What is React?", content: "React is a JavaScript library for building user interfaces, created by Facebook. It uses a component-based architecture where UIs are broken into reusable, self-contained pieces. React uses a Virtual DOM for efficient updates — it compares the new UI with the previous one and only updates what changed. JSX, a syntax extension, lets you write HTML-like code in JavaScript.", codeExamples: [{ language: "jsx", code: 'import React from "react";\n\nfunction App() {\n  return (\n    <div>\n      <h1>Hello, React!</h1>\n      <p>Welcome to my first React app.</p>\n    </div>\n  );\n}\n\nexport default App;', explanation: "A simple React component using JSX." }, { language: "jsx", code: '// JSX is JavaScript\nconst name = "Ali";\nconst element = <h1>Hello, {name}!</h1>;\n\n// JSX compiles to\nconst element2 = React.createElement("h1", null, `Hello, ${name}!`);', explanation: "JSX expressions can embed any JavaScript inside curly braces." }], exercises: [{ question: "Create a component that displays your name, age, and a greeting message.", solution: 'function Profile() {\n  const name = "Ahmed";\n  const age = 22;\n  return (\n    <div>\n      <h1>Hello, {name}!</h1>\n      <p>You are {age} years old.</p>\n    </div>\n  );\n}', difficulty: "beginner" }], externalLinks: [{ title: "React Official Docs", url: "https://react.dev/" }, { title: "W3Schools React", url: "https://www.w3schools.com/react/" }], estimatedMinutes: 20 },
    { id: "react-2", title: "Components & Props", content: "Components are the building blocks of React apps. They accept inputs called props (properties) and return JSX describing the UI. Props are read-only — a component should never modify its own props. Use destructuring to extract props. Components can be functions (preferred) or classes. Compose complex UIs by nesting components.", codeExamples: [{ language: "jsx", code: '// Reusable Card component\nfunction Card({ title, description, icon }) {\n  return (\n    <div className="card">\n      <span className="icon">{icon}</span>\n      <h3>{title}</h3>\n      <p>{description}</p>\n    </div>\n  );\n}\n\n// Using the component\nfunction App() {\n  return (\n    <div>\n      <Card title="HTML" description="Build web pages" icon="🌐" />\n      <Card title="CSS" description="Style web pages" icon="🎨" />\n      <Card title="JS" description="Add interactivity" icon="⚡" />\n    </div>\n  );\n}', explanation: "Reusable Card component with props." }], exercises: [{ question: "Create a UserCard component that shows name, email, and avatar.", solution: 'function UserCard({ name, email, avatar }) {\n  return (\n    <div className="user-card">\n      <img src={avatar} alt={name} />\n      <h3>{name}</h3>\n      <p>{email}</p>\n    </div>\n  );\n}', difficulty: "beginner" }], externalLinks: [{ title: "React Components & Props", url: "https://react.dev/learn/passing-props-to-a-component" }], estimatedMinutes: 20 },
    { id: "react-3", title: "State with useState", content: "State is data that changes over time. useState is a React Hook that adds state to functional components. It returns [value, setter]. When state changes, React re-renders the component. State is local to each component instance. Never mutate state directly — always use the setter function. For state based on previous value, use the callback form.", codeExamples: [{ language: "jsx", code: 'import { useState } from "react";\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  return (\n    <div>\n      <p>Count: {count}</p>\n      <button onClick={() => setCount(count + 1)}>+</button>\n      <button onClick={() => setCount(count - 1)}>-</button>\n      <button onClick={() => setCount(0)}>Reset</button>\n    </div>\n  );\n}\n\n// Object state\nfunction Form() {\n  const [form, setForm] = useState({ name: "", email: "" });\n  \n  const updateField = (field, value) => {\n    setForm(prev => ({ ...prev, [field]: value }));\n  };\n  \n  return (\n    <>\n      <input value={form.name} onChange={e => updateField("name", e.target.value)} />\n      <input value={form.email} onChange={e => updateField("email", e.target.value)} />\n    </>\n  );\n}', explanation: "Counter and form with useState hook." }], exercises: [{ question: "Create a toggle component that switches between light and dark mode text.", solution: 'function ThemeToggle() {\n  const [isDark, setIsDark] = useState(false);\n  return (\n    <div style={{ background: isDark ? "#333" : "#fff", color: isDark ? "#fff" : "#333", padding: 20 }}>\n      <p>{isDark ? "Dark Mode" : "Light Mode"}</p>\n      <button onClick={() => setIsDark(!isDark)}>Toggle</button>\n    </div>\n  );\n}', difficulty: "beginner" }], externalLinks: [{ title: "React useState", url: "https://react.dev/reference/react/useState" }], estimatedMinutes: 25 },
    { id: "react-4", title: "Handling Events", content: "React events use camelCase (onClick, onChange, onSubmit) and pass functions, not strings. Event handlers receive a synthetic event object with methods like preventDefault() and stopPropagation(). Use arrow functions for inline handlers or define named functions for reuse. Form handling combines onChange for inputs and onSubmit for the form.", codeExamples: [{ language: "jsx", code: 'function LoginForm() {\n  const [email, setEmail] = useState("");\n  const [password, setPassword] = useState("");\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    console.log("Login:", { email, password });\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input\n        type="email"\n        value={email}\n        onChange={(e) => setEmail(e.target.value)}\n        placeholder="Email"\n      />\n      <input\n        type="password"\n        value={password}\n        onChange={(e) => setPassword(e.target.value)}\n        placeholder="Password"\n      />\n      <button type="submit">Login</button>\n    </form>\n  );\n}', explanation: "Controlled form with event handling." }], exercises: [{ question: "Create a form with name and message fields that shows a preview below.", solution: 'function MessageForm() {\n  const [name, setName] = useState("");\n  const [message, setMessage] = useState("");\n  return (\n    <div>\n      <input value={name} onChange={e => setName(e.target.value)} placeholder="Name" />\n      <textarea value={message} onChange={e => setMessage(e.target.value)} placeholder="Message" />\n      <div>\n        <strong>{name}</strong>: {message}\n      </div>\n    </div>\n  );\n}', difficulty: "beginner" }], externalLinks: [{ title: "React Events", url: "https://react.dev/learn/responding-to-events" }], estimatedMinutes: 20 },
    { id: "react-5", title: "Conditional Rendering", content: "React components can render different output based on conditions. Use ternary operators (condition ? a : b), logical AND (condition && element), or early returns. For multiple conditions, switch statements or object maps work well. null or false renders nothing.", codeExamples: [{ language: "jsx", code: 'function Dashboard({ user, notifications }) {\n  if (!user) return <p>Please log in</p>;\n\n  return (\n    <div>\n      <h1>Welcome, {user.name}</h1>\n\n      {/* Ternary */}\n      {user.isPremium ? (\n        <span className="badge">Premium</span>\n      ) : (\n        <button>Upgrade</button>\n      )}\n\n      {/* Logical AND */}\n      {notifications.length > 0 && (\n        <div className="alert">\n          You have {notifications.length} notifications\n        </div>\n      )}\n    </div>\n  );\n}', explanation: "Conditional rendering patterns in React." }], exercises: [{ question: "Create a component that shows loading, error, or data states.", solution: 'function DataDisplay({ isLoading, error, data }) {\n  if (isLoading) return <p>Loading...</p>;\n  if (error) return <p>Error: {error}</p>;\n  if (!data) return <p>No data available</p>;\n  return <div>{JSON.stringify(data)}</div>;\n}', difficulty: "beginner" }], externalLinks: [{ title: "React Conditional Rendering", url: "https://react.dev/learn/conditional-rendering" }], estimatedMinutes: 15 },
    { id: "react-6", title: "Lists & Keys", content: "Render lists using Array.map() to transform data into JSX elements. Each item needs a unique key prop for React to efficiently update the DOM. Keys should be stable IDs, not array indices (unless list is static). Never use Math.random() as keys. Keys help React identify which items changed, were added, or removed.", codeExamples: [{ language: "jsx", code: 'function TodoList() {\n  const [todos, setTodos] = useState([\n    { id: 1, text: "Learn React", done: false },\n    { id: 2, text: "Build a project", done: false },\n  ]);\n\n  const toggleTodo = (id) => {\n    setTodos(todos.map(todo =>\n      todo.id === id ? { ...todo, done: !todo.done } : todo\n    ));\n  };\n\n  return (\n    <ul>\n      {todos.map(todo => (\n        <li\n          key={todo.id}\n          onClick={() => toggleTodo(todo.id)}\n          style={{ textDecoration: todo.done ? "line-through" : "none" }}\n        >\n          {todo.text}\n        </li>\n      ))}\n    </ul>\n  );\n}', explanation: "Rendering and interacting with lists using keys." }], exercises: [{ question: "Create a list component that renders students and allows removing them.", solution: 'function StudentList() {\n  const [students, setStudents] = useState([\n    { id: 1, name: "Ali" }, { id: 2, name: "Sara" }\n  ]);\n  const remove = (id) => setStudents(s => s.filter(s => s.id !== id));\n  return (\n    <ul>\n      {students.map(s => (\n        <li key={s.id}>{s.name} <button onClick={() => remove(s.id)}>×</button></li>\n      ))}\n    </ul>\n  );\n}', difficulty: "beginner" }], externalLinks: [{ title: "React Lists & Keys", url: "https://react.dev/learn/rendering-lists" }], estimatedMinutes: 20 },
    { id: "react-7", title: "useEffect Hook", content: "useEffect handles side effects: API calls, subscriptions, timers, and DOM manipulation. It runs after render. The dependency array controls when it runs: [] (once on mount), [dep] (when dep changes), no array (every render). Return a cleanup function for subscriptions/timers. Avoid infinite loops by managing dependencies correctly.", codeExamples: [{ language: "jsx", code: 'import { useState, useEffect } from "react";\n\nfunction UserProfile({ userId }) {\n  const [user, setUser] = useState(null);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    setLoading(true);\n    fetch(`/api/users/${userId}`)\n      .then(res => res.json())\n      .then(data => {\n        setUser(data);\n        setLoading(false);\n      });\n\n    // Cleanup (optional)\n    return () => console.log("Cleanup for", userId);\n  }, [userId]); // Re-run when userId changes\n\n  if (loading) return <p>Loading...</p>;\n  return <h1>{user?.name}</h1>;\n}', explanation: "useEffect for data fetching with loading state." }], exercises: [{ question: "Create a component that fetches and displays a random quote on mount.", solution: 'function RandomQuote() {\n  const [quote, setQuote] = useState("");\n  useEffect(() => {\n    fetch("https://api.quotable.io/random")\n      .then(r => r.json())\n      .then(data => setQuote(data.content));\n  }, []);\n  return <blockquote>{quote || "Loading..."}</blockquote>;\n}', difficulty: "beginner" }], externalLinks: [{ title: "React useEffect", url: "https://react.dev/reference/react/useEffect" }], estimatedMinutes: 25 },
    { id: "react-8", title: "Styling React Components", content: "React supports multiple styling approaches: CSS classes, inline styles (objects), CSS Modules (scoped), Tailwind CSS (utility-first), and CSS-in-JS (styled-components). className replaces class in JSX. Inline styles use camelCase properties. CSS Modules prevent name collisions. Tailwind is popular for rapid development.", codeExamples: [{ language: "jsx", code: '// Tailwind CSS (most popular)\nfunction Card({ title, children }) {\n  return (\n    <div className="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition-shadow">\n      <h3 className="text-lg font-bold text-gray-900 mb-2">{title}</h3>\n      <div className="text-gray-600">{children}</div>\n    </div>\n  );\n}\n\n// Conditional classes\nfunction Button({ variant = "primary", children, ...props }) {\n  const baseClasses = "px-4 py-2 rounded-lg font-medium transition-colors";\n  const variants = {\n    primary: "bg-blue-600 text-white hover:bg-blue-700",\n    secondary: "bg-gray-200 text-gray-800 hover:bg-gray-300",\n    danger: "bg-red-600 text-white hover:bg-red-700",\n  };\n  return (\n    <button className={`${baseClasses} ${variants[variant]}`} {...props}>\n      {children}\n    </button>\n  );\n}', explanation: "Styling components with Tailwind CSS and conditional classes." }], exercises: [{ question: "Create a styled badge component with different color variants.", solution: 'function Badge({ text, variant = "default" }) {\n  const colors = {\n    default: "bg-gray-100 text-gray-800",\n    success: "bg-green-100 text-green-800",\n    warning: "bg-yellow-100 text-yellow-800",\n    error: "bg-red-100 text-red-800",\n  };\n  return <span className={`px-2 py-1 rounded-full text-xs font-medium ${colors[variant]}`}>{text}</span>;\n}', difficulty: "beginner" }], externalLinks: [{ title: "Tailwind CSS Docs", url: "https://tailwindcss.com/docs" }], estimatedMinutes: 20 },
    { id: "react-9", title: "Component Composition", content: "Composition is React's primary pattern for code reuse. Use the children prop to pass JSX into components. Create layout components, wrapper components, and compound components. Favor composition over inheritance. The children prop makes components flexible and reusable. Render props and slots enable advanced composition patterns.", codeExamples: [{ language: "jsx", code: '// Layout component\nfunction PageLayout({ children, sidebar }) {\n  return (\n    <div className="flex min-h-screen">\n      <aside className="w-64 bg-gray-50 p-4">{sidebar}</aside>\n      <main className="flex-1 p-6">{children}</main>\n    </div>\n  );\n}\n\n// Wrapper component\nfunction ErrorBoundary({ children, fallback }) {\n  const [hasError, setHasError] = useState(false);\n  if (hasError) return fallback || <p>Something went wrong</p>;\n  return children;\n}\n\n// Usage\nfunction App() {\n  return (\n    <PageLayout sidebar={<NavMenu />}>\n      <h1>Dashboard</h1>\n      <ErrorBoundary fallback={<p>Failed to load</p>}>\n        <DataWidget />\n      </ErrorBoundary>\n    </PageLayout>\n  );\n}', explanation: "Layout and wrapper components using children prop." }], exercises: [{ question: "Create a Modal component that accepts title and children content.", solution: 'function Modal({ isOpen, onClose, title, children }) {\n  if (!isOpen) return null;\n  return (\n    <div className="fixed inset-0 bg-black/50 flex items-center justify-center">\n      <div className="bg-white rounded-xl p-6 max-w-md w-full">\n        <div className="flex justify-between mb-4">\n          <h2 className="text-lg font-bold">{title}</h2>\n          <button onClick={onClose}>×</button>\n        </div>\n        {children}\n      </div>\n    </div>\n  );\n}', difficulty: "beginner" }], externalLinks: [{ title: "React Composition", url: "https://react.dev/learn/passing-props-to-a-component#passing-jsx-as-children" }], estimatedMinutes: 20 },
  ],
  intermediate: [
    { id: "react-10", title: "Context API & Global State", content: "React Context provides a way to share data across components without prop drilling. Create context with createContext(), provide values with Provider, consume with useContext(). Great for themes, auth, locale. Don't overuse — it re-renders all consumers on change. For complex state, consider useReducer with Context.", codeExamples: [{ language: "jsx", code: 'import { createContext, useContext, useState } from "react";\n\n// Create context\nconst ThemeContext = createContext();\n\n// Provider component\nfunction ThemeProvider({ children }) {\n  const [theme, setTheme] = useState("light");\n  const toggle = () => setTheme(t => t === "light" ? "dark" : "light");\n  return (\n    <ThemeContext.Provider value={{ theme, toggle }}>\n      {children}\n    </ThemeContext.Provider>\n  );\n}\n\n// Custom hook\nconst useTheme = () => useContext(ThemeContext);\n\n// Consumer component\nfunction Header() {\n  const { theme, toggle } = useTheme();\n  return (\n    <header className={theme === "dark" ? "bg-gray-900" : "bg-white"}>\n      <button onClick={toggle}>Toggle Theme</button>\n    </header>\n  );\n}', explanation: "Theme context with provider, hook, and consumer." }], exercises: [{ question: "Create an AuthContext that provides user data and login/logout functions.", solution: 'const AuthContext = createContext();\nfunction AuthProvider({ children }) {\n  const [user, setUser] = useState(null);\n  const login = (userData) => setUser(userData);\n  const logout = () => setUser(null);\n  return (\n    <AuthContext.Provider value={{ user, login, logout }}>\n      {children}\n    </AuthContext.Provider>\n  );\n}\nconst useAuth = () => useContext(AuthContext);', difficulty: "intermediate" }], externalLinks: [{ title: "React Context", url: "https://react.dev/learn/passing-data-deeply-with-context" }], estimatedMinutes: 25 },
    { id: "react-11", title: "useReducer for Complex State", content: "useReducer is an alternative to useState for complex state logic. It follows the Redux pattern: dispatch actions to a reducer function that returns new state. Use when state has multiple sub-values, complex update logic, or when next state depends on previous state. Combine with Context for global state management.", codeExamples: [{ language: "jsx", code: 'import { useReducer } from "react";\n\nconst initialState = { todos: [], filter: "all" };\n\nfunction todoReducer(state, action) {\n  switch (action.type) {\n    case "ADD_TODO":\n      return { ...state, todos: [...state.todos, { id: Date.now(), text: action.text, done: false }] };\n    case "TOGGLE_TODO":\n      return { ...state, todos: state.todos.map(t => t.id === action.id ? { ...t, done: !t.done } : t) };\n    case "DELETE_TODO":\n      return { ...state, todos: state.todos.filter(t => t.id !== action.id) };\n    case "SET_FILTER":\n      return { ...state, filter: action.filter };\n    default:\n      return state;\n  }\n}\n\nfunction TodoApp() {\n  const [state, dispatch] = useReducer(todoReducer, initialState);\n  return (\n    <div>\n      <button onClick={() => dispatch({ type: "ADD_TODO", text: "New task" })}>Add</button>\n      {state.todos.map(todo => (\n        <div key={todo.id} onClick={() => dispatch({ type: "TOGGLE_TODO", id: todo.id })}>\n          {todo.text}\n        </div>\n      ))}\n    </div>\n  );\n}', explanation: "Todo app with useReducer for predictable state updates." }], exercises: [{ question: "Create a shopping cart with add, remove, and update quantity using useReducer.", solution: 'function cartReducer(state, action) {\n  switch (action.type) {\n    case "ADD": return [...state, { ...action.item, qty: 1 }];\n    case "REMOVE": return state.filter(i => i.id !== action.id);\n    case "UPDATE_QTY": return state.map(i => i.id === action.id ? { ...i, qty: action.qty } : i);\n    default: return state;\n  }\n}', difficulty: "intermediate" }], externalLinks: [{ title: "React useReducer", url: "https://react.dev/reference/react/useReducer" }], estimatedMinutes: 25 },
    { id: "react-12", title: "React Router", content: "React Router enables client-side routing — navigation without page reloads. Use BrowserRouter, Routes, Route for defining routes. Link/NavLink for navigation. useNavigate for programmatic navigation. useParams for URL parameters. Nested routes for layouts. Protected routes for authentication.", codeExamples: [{ language: "jsx", code: 'import { BrowserRouter, Routes, Route, Link, useParams, useNavigate } from "react-router-dom";\n\nfunction App() {\n  return (\n    <BrowserRouter>\n      <nav>\n        <Link to="/">Home</Link>\n        <Link to="/courses">Courses</Link>\n      </nav>\n      <Routes>\n        <Route path="/" element={<Home />} />\n        <Route path="/courses" element={<Courses />} />\n        <Route path="/courses/:id" element={<CourseDetail />} />\n        <Route path="*" element={<NotFound />} />\n      </Routes>\n    </BrowserRouter>\n  );\n}\n\nfunction CourseDetail() {\n  const { id } = useParams();\n  const navigate = useNavigate();\n  return (\n    <div>\n      <h1>Course: {id}</h1>\n      <button onClick={() => navigate("/courses")}>Back</button>\n    </div>\n  );\n}', explanation: "React Router setup with navigation and URL parameters." }], exercises: [{ question: "Create a protected route that redirects to /login if user is not authenticated.", solution: 'function ProtectedRoute({ children }) {\n  const { user } = useAuth();\n  if (!user) return <Navigate to="/login" replace />;\n  return children;\n}\n// Usage: <Route path="/dashboard" element={<ProtectedRoute><Dashboard /></ProtectedRoute>} />', difficulty: "intermediate" }], externalLinks: [{ title: "React Router Docs", url: "https://reactrouter.com/" }], estimatedMinutes: 25 },
    { id: "react-13", title: "Custom Hooks", content: "Custom hooks extract reusable logic from components. They start with 'use' prefix and can call other hooks. Common patterns: useLocalStorage, useFetch, useDebounce, useMediaQuery, useClickOutside. Custom hooks make components cleaner and logic testable. They follow the same rules as built-in hooks.", codeExamples: [{ language: "jsx", code: '// useLocalStorage hook\nfunction useLocalStorage(key, initialValue) {\n  const [value, setValue] = useState(() => {\n    try {\n      const item = localStorage.getItem(key);\n      return item ? JSON.parse(item) : initialValue;\n    } catch { return initialValue; }\n  });\n\n  useEffect(() => {\n    localStorage.setItem(key, JSON.stringify(value));\n  }, [key, value]);\n\n  return [value, setValue];\n}\n\n// useFetch hook\nfunction useFetch(url) {\n  const [data, setData] = useState(null);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() => {\n    fetch(url)\n      .then(r => r.json())\n      .then(setData)\n      .catch(setError)\n      .finally(() => setLoading(false));\n  }, [url]);\n\n  return { data, loading, error };\n}\n\n// Usage\nfunction App() {\n  const [theme, setTheme] = useLocalStorage("theme", "dark");\n  const { data, loading } = useFetch("/api/courses");\n}', explanation: "Custom hooks for localStorage and data fetching." }], exercises: [{ question: "Create a useDebounce hook that delays a value update.", solution: 'function useDebounce(value, delay = 300) {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n  useEffect(() => {\n    const timer = setTimeout(() => setDebouncedValue(value), delay);\n    return () => clearTimeout(timer);\n  }, [value, delay]);\n  return debouncedValue;\n}', difficulty: "intermediate" }], externalLinks: [{ title: "React Custom Hooks", url: "https://react.dev/learn/reusing-logic-with-custom-hooks" }], estimatedMinutes: 25 },
    { id: "react-14", title: "Forms & Validation", content: "React forms can be controlled (React manages state) or uncontrolled (DOM manages state via refs). Libraries like React Hook Form and Zod simplify validation. Controlled components use value + onChange. Display validation errors conditionally. Use onBlur for field-level validation and onSubmit for form-level.", codeExamples: [{ language: "jsx", code: 'import { useState } from "react";\n\nfunction SignupForm() {\n  const [form, setForm] = useState({ name: "", email: "", password: "" });\n  const [errors, setErrors] = useState({});\n\n  const validate = () => {\n    const errs = {};\n    if (!form.name) errs.name = "Name is required";\n    if (!form.email.includes("@")) errs.email = "Invalid email";\n    if (form.password.length < 8) errs.password = "Min 8 characters";\n    return errs;\n  };\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    const errs = validate();\n    if (Object.keys(errs).length > 0) {\n      setErrors(errs);\n      return;\n    }\n    console.log("Submit:", form);\n  };\n\n  const update = (field) => (e) => {\n    setForm(prev => ({ ...prev, [field]: e.target.value }));\n    setErrors(prev => ({ ...prev, [field]: undefined }));\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input value={form.name} onChange={update("name")} placeholder="Name" />\n      {errors.name && <span className="error">{errors.name}</span>}\n      <input value={form.email} onChange={update("email")} placeholder="Email" />\n      {errors.email && <span className="error">{errors.email}</span>}\n      <input type="password" value={form.password} onChange={update("password")} />\n      {errors.password && <span className="error">{errors.password}</span>}\n      <button type="submit">Sign Up</button>\n    </form>\n  );\n}', explanation: "Form validation with error messages." }], exercises: [{ question: "Add real-time validation that shows errors as the user types.", solution: 'const update = (field) => (e) => {\n  const value = e.target.value;\n  setForm(prev => ({ ...prev, [field]: value }));\n  // Real-time validation\n  if (field === "email" && value && !value.includes("@")) {\n    setErrors(prev => ({ ...prev, email: "Invalid email" }));\n  } else {\n    setErrors(prev => ({ ...prev, [field]: undefined }));\n  }\n};', difficulty: "intermediate" }], externalLinks: [{ title: "React Hook Form", url: "https://react-hook-form.com/" }], estimatedMinutes: 25 },
    { id: "react-15", title: "Data Fetching with React Query", content: "React Query (TanStack Query) simplifies server state management. It handles caching, background updates, stale data, pagination, and optimistic updates. useQuery for fetching data, useMutation for creating/updating/deleting. Automatic refetching on window focus and network reconnect.", codeExamples: [{ language: "jsx", code: 'import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";\n\nfunction CourseList() {\n  const { data: courses, isLoading, error } = useQuery({\n    queryKey: ["courses"],\n    queryFn: () => fetch("/api/courses").then(r => r.json()),\n  });\n\n  const queryClient = useQueryClient();\n  const deleteMutation = useMutation({\n    mutationFn: (id) => fetch(`/api/courses/${id}`, { method: "DELETE" }),\n    onSuccess: () => queryClient.invalidateQueries({ queryKey: ["courses"] }),\n  });\n\n  if (isLoading) return <p>Loading...</p>;\n  if (error) return <p>Error: {error.message}</p>;\n\n  return (\n    <ul>\n      {courses.map(course => (\n        <li key={course.id}>\n          {course.title}\n          <button onClick={() => deleteMutation.mutate(course.id)}>Delete</button>\n        </li>\n      ))}\n    </ul>\n  );\n}', explanation: "React Query for data fetching with caching and mutations." }], exercises: [{ question: "Add optimistic updates to a mutation that updates a course title.", solution: 'const updateMutation = useMutation({\n  mutationFn: ({ id, title }) => fetch(`/api/courses/${id}`, {\n    method: "PATCH",\n    body: JSON.stringify({ title }),\n  }),\n  onMutate: async ({ id, title }) => {\n    await queryClient.cancelQueries({ queryKey: ["courses"] });\n    const previous = queryClient.getQueryData(["courses"]);\n    queryClient.setQueryData(["courses"], old =>\n      old.map(c => c.id === id ? { ...c, title } : c)\n    );\n    return { previous };\n  },\n  onError: (err, vars, context) => queryClient.setQueryData(["courses"], context.previous),\n  onSettled: () => queryClient.invalidateQueries({ queryKey: ["courses"] }),\n});', difficulty: "intermediate" }], externalLinks: [{ title: "TanStack Query", url: "https://tanstack.com/query/latest" }], estimatedMinutes: 30 },
    { id: "react-16", title: "useMemo & useCallback", content: "useMemo caches computed values to avoid expensive recalculations on every render. useCallback caches function references to prevent unnecessary re-renders of child components. Use when: heavy computations in render, passing callbacks to memoized children, referential equality matters. Don't premature optimize — profile first.", codeExamples: [{ language: "jsx", code: 'import { useMemo, useCallback, memo } from "react";\n\nfunction SearchResults({ items, query }) {\n  // useMemo: cache filtered results\n  const filtered = useMemo(\n    () => items.filter(item => item.name.toLowerCase().includes(query.toLowerCase())),\n    [items, query]\n  );\n\n  // useCallback: stable function reference\n  const handleSelect = useCallback((id) => {\n    console.log("Selected:", id);\n  }, []);\n\n  return (\n    <ul>\n      {filtered.map(item => (\n        <ListItem key={item.id} item={item} onSelect={handleSelect} />\n      ))}\n    </ul>\n  );\n}\n\n// memo: skip re-render if props unchanged\nconst ListItem = memo(({ item, onSelect }) => (\n  <li onClick={() => onSelect(item.id)}>{item.name}</li>\n));', explanation: "Performance hooks and React.memo for optimization." }], exercises: [{ question: "Optimize a large list component using useMemo and memo.", solution: 'const ExpensiveList = memo(({ items, onSelect }) => {\n  const sorted = useMemo(() => [...items].sort((a, b) => a.name.localeCompare(b.name)), [items]);\n  return sorted.map(item => <div key={item.id} onClick={() => onSelect(item.id)}>{item.name}</div>);\n});', difficulty: "intermediate" }], externalLinks: [{ title: "React useMemo", url: "https://react.dev/reference/react/useMemo" }], estimatedMinutes: 25 },
    { id: "react-17", title: "Error Boundaries", content: "Error boundaries catch JavaScript errors in child components and display fallback UI. Class components with componentDidCatch lifecycle method. For functional components, use react-error-boundary library. Error boundaries catch render errors, not event handlers or async code. Always wrap critical sections.", codeExamples: [{ language: "jsx", code: 'import { ErrorBoundary } from "react-error-boundary";\n\nfunction ErrorFallback({ error, resetErrorBoundary }) {\n  return (\n    <div className="p-4 bg-red-50 rounded-lg">\n      <h2 className="text-red-800 font-bold">Something went wrong</h2>\n      <p className="text-red-600 text-sm">{error.message}</p>\n      <button onClick={resetErrorBoundary} className="mt-2 px-4 py-2 bg-red-600 text-white rounded">\n        Try again\n      </button>\n    </div>\n  );\n}\n\nfunction App() {\n  return (\n    <ErrorBoundary FallbackComponent={ErrorFallback} onReset={() => window.location.reload()}>\n      <Dashboard />\n    </ErrorBoundary>\n  );\n}', explanation: "Error boundaries with fallback UI and reset functionality." }], exercises: [{ question: "Wrap a data-fetching component in an error boundary with retry logic.", solution: 'function App() {\n  return (\n    <ErrorBoundary\n      FallbackComponent={ErrorFallback}\n      onReset={() => queryClient.invalidateQueries()}\n    >\n      <DataComponent />\n    </ErrorBoundary>\n  );\n}', difficulty: "intermediate" }], externalLinks: [{ title: "React Error Boundaries", url: "https://react.dev/reference/react/Component#catching-rendering-errors-with-an-error-boundary" }], estimatedMinutes: 20 },
    { id: "react-18", title: "Refs & useRef", content: "useRef creates a mutable reference that persists across renders without causing re-renders. Common uses: accessing DOM elements directly, storing previous values, managing timers/intervals, and uncontrolled form inputs. ref.current holds the current value. Use forwardRef to pass refs to child components.", codeExamples: [{ language: "jsx", code: 'import { useRef, useEffect } from "react";\n\nfunction AutoFocusInput() {\n  const inputRef = useRef(null);\n\n  useEffect(() => {\n    inputRef.current?.focus();\n  }, []);\n\n  return <input ref={inputRef} placeholder="Auto-focused" />;\n}\n\n// Timer with ref\nfunction Stopwatch() {\n  const [time, setTime] = useState(0);\n  const intervalRef = useRef(null);\n\n  const start = () => {\n    intervalRef.current = setInterval(() => setTime(t => t + 1), 1000);\n  };\n  const stop = () => clearInterval(intervalRef.current);\n\n  return (\n    <div>\n      <p>{time}s</p>\n      <button onClick={start}>Start</button>\n      <button onClick={stop}>Stop</button>\n    </div>\n  );\n}', explanation: "useRef for DOM access and persisting values." }], exercises: [{ question: "Create a component that scrolls to a specific section when a button is clicked.", solution: 'function ScrollDemo() {\n  const sectionRef = useRef(null);\n  const scrollTo = () => sectionRef.current?.scrollIntoView({ behavior: "smooth" });\n  return (\n    <div>\n      <button onClick={scrollTo}>Scroll to section</button>\n      <div style={{ height: "100vh" }} />\n      <div ref={sectionRef}><h2>Target Section</h2></div>\n    </div>\n  );\n}', difficulty: "intermediate" }], externalLinks: [{ title: "React useRef", url: "https://react.dev/reference/react/useRef" }], estimatedMinutes: 20 },
  ],
  hard: [
    { id: "react-19", title: "Advanced State Patterns", content: "Complex apps need sophisticated state management. Patterns include: state machines (useReducer with strict transitions), compound components (shared state via context), render props, higher-order components (HOCs), and external stores (Zustand, Jotai). Choose based on complexity: local state → context → external store.", codeExamples: [{ language: "jsx", code: '// State machine with useReducer\nconst machine = {\n  idle: { FETCH: "loading" },\n  loading: { SUCCESS: "success", ERROR: "error" },\n  success: { FETCH: "loading" },\n  error: { FETCH: "loading", RESET: "idle" },\n};\n\nfunction reducer(state, event) {\n  const nextState = machine[state.status]?.[event.type];\n  if (!nextState) return state;\n  switch (nextState) {\n    case "loading": return { status: "loading", data: null, error: null };\n    case "success": return { status: "success", data: event.data, error: null };\n    case "error": return { status: "error", data: null, error: event.error };\n    default: return { status: nextState, data: null, error: null };\n  }\n}\n\nfunction useFetchMachine(url) {\n  const [state, dispatch] = useReducer(reducer, { status: "idle", data: null, error: null });\n  const fetchData = async () => {\n    dispatch({ type: "FETCH" });\n    try {\n      const res = await fetch(url);\n      const data = await res.json();\n      dispatch({ type: "SUCCESS", data });\n    } catch (error) {\n      dispatch({ type: "ERROR", error });\n    }\n  };\n  return { ...state, fetchData };\n}', explanation: "State machine pattern for predictable async state." }], exercises: [{ question: "Implement a multi-step form wizard using state machine pattern.", solution: 'const wizardMachine = {\n  step1: { NEXT: "step2" },\n  step2: { NEXT: "step3", BACK: "step1" },\n  step3: { NEXT: "complete", BACK: "step2" },\n  complete: { RESET: "step1" },\n};\nfunction wizardReducer(state, action) {\n  const next = wizardMachine[state.step]?.[action.type];\n  if (!next) return state;\n  return { ...state, step: next, data: { ...state.data, ...action.data } };\n}', difficulty: "advanced" }], externalLinks: [{ title: "XState", url: "https://xstate.js.org/" }], estimatedMinutes: 35 },
    { id: "react-20", title: "Performance Optimization", content: "React performance optimization involves: virtualization (rendering only visible items), code splitting (lazy loading routes/components), memoization (React.memo, useMemo, useCallback), avoiding unnecessary renders, using the React Profiler, and optimizing images/assets. Virtual lists handle thousands of items efficiently.", codeExamples: [{ language: "jsx", code: 'import { lazy, Suspense } from "react";\n\n// Code splitting with lazy loading\nconst Dashboard = lazy(() => import("./pages/Dashboard"));\nconst Analytics = lazy(() => import("./pages/Analytics"));\n\nfunction App() {\n  return (\n    <Suspense fallback={<div className="loading">Loading...</div>}>\n      <Routes>\n        <Route path="/" element={<Dashboard />} />\n        <Route path="/analytics" element={<Analytics />} />\n      </Routes>\n    </Suspense>\n  );\n}\n\n// Virtualized list for large data\nimport { FixedSizeList } from "react-window";\n\nfunction LargeList({ items }) {\n  const Row = ({ index, style }) => (\n    <div style={style}>{items[index].name}</div>\n  );\n  return (\n    <FixedSizeList height={400} itemCount={items.length} itemSize={50} width="100%">\n      {Row}\n    </FixedSizeList>\n  );\n}', explanation: "Code splitting and virtualization for performance." }], exercises: [{ question: "Implement lazy loading for a heavy chart component.", solution: 'const HeavyChart = lazy(() => import("./HeavyChart"));\nfunction Dashboard() {\n  return (\n    <Suspense fallback={<div>Loading chart...</div>}>\n      <HeavyChart data={chartData} />\n    </Suspense>\n  );\n}', difficulty: "advanced" }], externalLinks: [{ title: "React Profiler", url: "https://react.dev/reference/react/Profiler" }], estimatedMinutes: 30 },
    { id: "react-21", title: "Advanced Hooks: useTransition & useDeferredValue", content: "React 18 introduced concurrent features. useTransition marks state updates as non-urgent, keeping the UI responsive. useDeferredValue defers updating a value until higher-priority work is done. These are crucial for search/filter UIs where typing should feel instant even if results are slow to compute.", codeExamples: [{ language: "jsx", code: 'import { useState, useTransition, useDeferredValue } from "react";\n\nfunction SearchPage() {\n  const [query, setQuery] = useState("");\n  const [isPending, startTransition] = useTransition();\n  const deferredQuery = useDeferredValue(query);\n\n  const handleChange = (e) => {\n    setQuery(e.target.value); // Urgent: update input immediately\n    // Results update is deferred automatically via useDeferredValue\n  };\n\n  return (\n    <div>\n      <input value={query} onChange={handleChange} placeholder="Search..." />\n      <div style={{ opacity: isPending ? 0.5 : 1 }}>\n        <SearchResults query={deferredQuery} />\n      </div>\n    </div>\n  );\n}\n\nfunction SearchResults({ query }) {\n  // Expensive filtering\n  const results = useMemo(\n    () => allItems.filter(item => item.name.toLowerCase().includes(query.toLowerCase())),\n    [query]\n  );\n  return results.map(item => <div key={item.id}>{item.name}</div>);\n}', explanation: "Concurrent React features for responsive search UI." }], exercises: [{ question: "Use useTransition to make a tab switching UI that doesn't freeze during heavy rendering.", solution: 'function Tabs() {\n  const [tab, setTab] = useState("posts");\n  const [isPending, startTransition] = useTransition();\n  const selectTab = (next) => {\n    startTransition(() => setTab(next));\n  };\n  return (\n    <div>\n      <button onClick={() => selectTab("posts")}>Posts</button>\n      <button onClick={() => selectTab("comments")}>Comments {isPending && "..."}</button>\n      {tab === "posts" ? <Posts /> : <HeavyComments />}\n    </div>\n  );\n}', difficulty: "advanced" }], externalLinks: [{ title: "React useTransition", url: "https://react.dev/reference/react/useTransition" }], estimatedMinutes: 25 },
    { id: "react-22", title: "Testing React Components", content: "Test React components with React Testing Library (RTL) which tests from the user's perspective. Use render() to mount components, screen to query elements, fireEvent/userEvent for interactions, and expect for assertions. Test behavior, not implementation. Mock API calls with MSW (Mock Service Worker).", codeExamples: [{ language: "jsx", code: 'import { render, screen, fireEvent, waitFor } from "@testing-library/react";\nimport userEvent from "@testing-library/user-event";\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n  return (\n    <div>\n      <p>Count: {count}</p>\n      <button onClick={() => setCount(c => c + 1)}>Increment</button>\n    </div>\n  );\n}\n\ndescribe("Counter", () => {\n  it("increments on button click", async () => {\n    render(<Counter />);\n    expect(screen.getByText("Count: 0")).toBeInTheDocument();\n    await userEvent.click(screen.getByText("Increment"));\n    expect(screen.getByText("Count: 1")).toBeInTheDocument();\n  });\n\n  it("renders with initial value", () => {\n    render(<Counter />);\n    expect(screen.getByText(/Count: 0/)).toBeInTheDocument();\n  });\n});', explanation: "Testing React components with React Testing Library." }], exercises: [{ question: "Write tests for a LoginForm component that validates email and password.", solution: 'it("shows error for invalid email", async () => {\n  render(<LoginForm />);\n  await userEvent.type(screen.getByPlaceholderText("Email"), "invalid");\n  await userEvent.click(screen.getByText("Submit"));\n  expect(screen.getByText("Invalid email")).toBeInTheDocument();\n});', difficulty: "advanced" }], externalLinks: [{ title: "React Testing Library", url: "https://testing-library.com/docs/react-testing-library/intro/" }], estimatedMinutes: 30 },
    { id: "react-23", title: "Full-Stack React with Supabase", content: "Supabase provides a backend with database, auth, storage, and real-time subscriptions. Use @supabase/supabase-js client. Row Level Security (RLS) protects data. Combine with React Query for caching. Supabase auth handles signup, login, OAuth. Real-time subscriptions update UI instantly.", codeExamples: [{ language: "jsx", code: 'import { supabase } from "@/integrations/supabase/client";\nimport { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";\n\n// Fetch courses\nfunction useCourses() {\n  return useQuery({\n    queryKey: ["courses"],\n    queryFn: async () => {\n      const { data, error } = await supabase.from("courses").select("*");\n      if (error) throw error;\n      return data;\n    },\n  });\n}\n\n// Auth example\nasync function signUp(email, password) {\n  const { error } = await supabase.auth.signUp({ email, password });\n  if (error) throw error;\n}\n\n// Real-time subscription\nuseEffect(() => {\n  const channel = supabase\n    .channel("progress")\n    .on("postgres_changes", { event: "*", schema: "public", table: "user_course_progress" },\n      (payload) => console.log("Change:", payload)\n    )\n    .subscribe();\n  return () => { supabase.removeChannel(channel); };\n}, []);', explanation: "Supabase integration with React for full-stack features." }], exercises: [{ question: "Create a hook that subscribes to real-time updates for a specific table.", solution: 'function useRealtime(table, callback) {\n  useEffect(() => {\n    const channel = supabase\n      .channel(`realtime-${table}`)\n      .on("postgres_changes", { event: "*", schema: "public", table }, callback)\n      .subscribe();\n    return () => supabase.removeChannel(channel);\n  }, [table]);\n}', difficulty: "advanced" }], externalLinks: [{ title: "Supabase Docs", url: "https://supabase.com/docs" }], estimatedMinutes: 35 },
  ],
};
