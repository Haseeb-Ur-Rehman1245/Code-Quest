import { CourseLessons } from "./types";

export const deploymentLessons: CourseLessons = {
  easy: [
    { id: "dep-1", title: "Introduction to Deployment", content: "Deployment is the process of making your application available to users on the internet. Understanding deployment involves knowledge of hosting platforms, domain names, DNS, SSL certificates, and environment variables. Modern deployment options range from simple static hosting (Netlify, Vercel) to containerized deployments (Docker, Kubernetes). A good deployment pipeline ensures your code goes from development to production reliably and quickly.", codeExamples: [{ language: "bash", code: '# Static site deployment with Netlify CLI\nnpm install -g netlify-cli\nnpm run build\nnetlify deploy --prod --dir=dist\n\n# Vercel deployment\nnpm install -g vercel\nvercel --prod\n\n# GitHub Pages\nnpm run build\nnpx gh-pages -d dist', explanation: "Static site deployment to popular platforms." }, { language: "bash", code: '# Environment variables\n# .env (local development)\nDATABASE_URL=mongodb://localhost:27017/myapp\nJWT_SECRET=my-super-secret-key\nAPI_KEY=abc123\n\n# .env.example (committed to git, no real values)\nDATABASE_URL=\nJWT_SECRET=\nAPI_KEY=\n\n# NEVER commit .env to git!\n# .gitignore\n.env\n.env.local\n.env.production', explanation: "Managing environment variables securely." }, { language: "javascript", code: '// Build scripts in package.json\n{\n  "scripts": {\n    "dev": "vite",\n    "build": "tsc && vite build",\n    "preview": "vite preview",\n    "start": "node dist/server.js",\n    "lint": "eslint src/",\n    "test": "vitest run",\n    "predeploy": "npm run build",\n    "deploy": "netlify deploy --prod --dir=dist"\n  }\n}', explanation: "Build scripts and pre-deployment checklist." }], exercises: [{ question: "Deploy a React app to Vercel with environment variables configured.", solution: '# 1. Push to GitHub\ngit add . && git commit -m "Ready" && git push\n# 2. Connect to Vercel\nvercel link\n# 3. Set env vars\nvercel env add DATABASE_URL production\n# 4. Deploy\nvercel --prod', difficulty: "beginner" }], externalLinks: [{ title: "Vercel Deployment", url: "https://vercel.com/docs" }], estimatedMinutes: 20 },
    { id: "dep-2", title: "Domain Names & DNS", content: "A domain name is a human-readable address for your website. DNS translates domain names to IP addresses. Understanding DNS records is essential: A records point to IPv4 addresses, AAAA to IPv6, CNAME creates aliases, MX handles email, and TXT stores verification data. Proper DNS configuration with SSL/TLS certificates ensures your site is accessible and secure.", codeExamples: [{ language: "bash", code: '# DNS Record Types\n# A Record — Points domain to IPv4 address\nexample.com    A    192.168.1.1\n\n# CNAME — Alias to another domain\nwww.example.com    CNAME    example.com\napi.example.com    CNAME    my-api.herokuapp.com\n\n# MX — Email routing\nexample.com    MX    10 mail.example.com\n\n# Lookup DNS records\nnslookup example.com\ndig example.com A', explanation: "DNS record types and lookup commands." }, { language: "bash", code: '# SSL/TLS with Let\'s Encrypt\nsudo apt install certbot python3-certbot-nginx\nsudo certbot --nginx -d example.com -d www.example.com\nsudo certbot renew --dry-run\n\n# Force HTTPS redirect (nginx)\nserver {\n    listen 80;\n    server_name example.com;\n    return 301 https://$host$request_uri;\n}', explanation: "SSL certificate setup with Let's Encrypt." }, { language: "javascript", code: '// Custom domain setup for common platforms\n// Vercel: Settings > Domains > Add domain\n//   A Record: 76.76.21.21\n//   CNAME: cname.vercel-dns.com (for www)\n// Netlify: Domain Settings > Add custom domain\n//   A Record: 75.2.60.5\n// SSL is automatic on both platforms!\n\n// Health check endpoint\napp.get("/health", (req, res) => {\n  res.json({ status: "healthy", timestamp: new Date().toISOString() });\n});', explanation: "Custom domain configuration." }], exercises: [{ question: "Set up a custom domain with HTTPS for a deployed application.", solution: '// 1. Buy domain from registrar\n// 2. Add domain to hosting platform\n// 3. Configure DNS: A Record → platform IP, CNAME: www → platform domain\n// 4. Wait for DNS propagation (up to 48 hours)\n// 5. Verify SSL certificate is active', difficulty: "beginner" }], externalLinks: [{ title: "DNS Explained", url: "https://www.cloudflare.com/learning/dns/what-is-dns/" }], estimatedMinutes: 20 },
    { id: "dep-3", title: "Hosting Platforms Compared", content: "Modern hosting platforms offer different trade-offs between simplicity, control, and cost. Vercel excels for frontend and Next.js apps. Netlify is great for JAMstack. Railway and Render provide easy full-stack hosting. Heroku pioneered PaaS but has become more expensive. AWS, GCP, and Azure offer maximum control but require more DevOps knowledge.", codeExamples: [{ language: "bash", code: '# Railway deployment\nnpm install -g @railway/cli\nrailway login\nrailway init\nrailway up\n\n# Railway provisions database automatically\nrailway add postgresql\nrailway add mongodb\n\n# Environment variables are auto-injected\n# DATABASE_URL, MONGO_URL, etc.\n\n# Render deployment\n# 1. Connect GitHub repo to render.com\n# 2. Select "Web Service"\n# 3. Set build command: npm install && npm run build\n# 4. Set start command: npm start\n# 5. Add environment variables in dashboard', explanation: "Railway and Render deployment." }, { language: "yaml", code: '# render.yaml — Infrastructure as Code\nservices:\n  - type: web\n    name: my-api\n    env: node\n    plan: free\n    buildCommand: npm install && npm run build\n    startCommand: npm start\n    envVars:\n      - key: NODE_ENV\n        value: production\n      - key: DATABASE_URL\n        fromDatabase:\n          name: mydb\n          property: connectionString\n\ndatabases:\n  - name: mydb\n    plan: free', explanation: "Render infrastructure as code." }, { language: "javascript", code: '// Platform comparison\nconst platforms = {\n  vercel: { best: "Next.js, React", free: "Hobby tier", db: "No (use external)" },\n  netlify: { best: "Static sites, JAMstack", free: "100GB/mo bandwidth", db: "No" },\n  railway: { best: "Full-stack + DB", free: "$5 credit/mo", db: "Postgres, MongoDB, Redis" },\n  render: { best: "Full-stack + DB", free: "750 hours/mo", db: "Postgres" },\n  fly: { best: "Docker apps, edge", free: "3 shared VMs", db: "Postgres" },\n  aws: { best: "Enterprise, full control", free: "12-month free tier", db: "Everything" }\n};', explanation: "Platform comparison for different use cases." }], exercises: [{ question: "Deploy a full-stack app with a backend on Railway and frontend on Vercel.", solution: '// Backend (Railway):\n// 1. railway init → link GitHub repo\n// 2. railway add postgresql → provision DB\n// 3. Set env vars: JWT_SECRET, CORS_ORIGIN\n// 4. railway up → deploy\n\n// Frontend (Vercel):\n// 1. vercel link → connect repo\n// 2. Set env: VITE_API_URL=https://api.railway.app\n// 3. vercel --prod → deploy\n\n// Configure CORS on backend for Vercel domain', difficulty: "beginner" }], externalLinks: [{ title: "Railway Docs", url: "https://docs.railway.app/" }], estimatedMinutes: 20 },
    { id: "dep-4", title: "Static vs Dynamic Hosting", content: "Static hosting serves pre-built HTML/CSS/JS files from a CDN — fast, cheap, and scalable. Dynamic hosting runs a server process that generates responses on-demand — required for server-side rendering, APIs, and database access. Understanding which you need determines your hosting choice and architecture. Many modern apps use a hybrid approach: static frontend with a dynamic API backend.", codeExamples: [{ language: "bash", code: '# Static hosting (CDN-based)\n# Build once, serve everywhere\nnpm run build  # generates /dist folder\n\n# Netlify: drag-and-drop /dist folder\n# Vercel: auto-detects build output\n# CloudFlare Pages: connect GitHub, auto-deploy\n# GitHub Pages: push to gh-pages branch\n\n# Static site benefits:\n# - Instant page loads (served from nearest CDN edge)\n# - No server to maintain\n# - Scales to millions of users automatically\n# - Free or very cheap hosting\n\n# Limitations:\n# - No server-side code execution\n# - No database queries at request time\n# - Content changes require rebuild + redeploy', explanation: "Static hosting benefits and limitations." }, { language: "bash", code: '# Dynamic hosting (server process)\n# Server runs continuously, handles requests\n\n# Node.js server\nnode server.js  # runs on port 3000\n\n# PM2 process manager (production)\nnpm install -g pm2\npm2 start server.js --name myapp -i max  # cluster mode\npm2 monit    # real-time monitoring\npm2 logs     # view logs\npm2 restart  # restart app\npm2 save     # save process list\npm2 startup  # auto-start on reboot', explanation: "Dynamic hosting with PM2 process manager." }, { language: "javascript", code: '// Hybrid architecture: static frontend + API backend\n\n// Frontend (static, on Vercel/Netlify)\nconst API_URL = import.meta.env.VITE_API_URL;\nconst users = await fetch(`${API_URL}/api/users`).then(r => r.json());\n\n// Backend (dynamic, on Railway/Render)\nconst express = require("express");\nconst app = express();\napp.use(cors({ origin: process.env.FRONTEND_URL }));\napp.get("/api/users", async (req, res) => {\n  const users = await db.query("SELECT * FROM users");\n  res.json(users);\n});\napp.listen(process.env.PORT || 3000);', explanation: "Hybrid static frontend + dynamic backend architecture." }], exercises: [{ question: "Determine whether a project needs static or dynamic hosting and set up the appropriate deployment.", solution: '// Decision tree:\n// Does the app need a database? → Dynamic backend needed\n// Does it need SSR? → Dynamic hosting (or serverless)\n// Is it a SPA with external API? → Static hosting works\n// Does it need real-time features? → Dynamic with WebSocket\n\n// Example: Blog with CMS\n// Frontend: Static (Vercel) — pre-built React SPA\n// Backend: Dynamic (Railway) — Express + PostgreSQL\n// Advantage: frontend scales via CDN, backend scales independently', difficulty: "beginner" }], externalLinks: [{ title: "JAMstack Architecture", url: "https://jamstack.org/" }], estimatedMinutes: 20 },
  ],
  intermediate: [
    { id: "dep-5", title: "Docker Containerization", content: "Docker packages applications and their dependencies into containers — lightweight, portable environments that run consistently everywhere. A Dockerfile defines how to build a container image. Docker Compose manages multi-container applications. Containers solve the 'works on my machine' problem by ensuring identical environments across development, testing, and production.", codeExamples: [{ language: "dockerfile", code: '# Multi-stage build for Node.js app\nFROM node:18-alpine AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --only=production\n\nFROM node:18-alpine\nWORKDIR /app\nCOPY --from=builder /app/node_modules ./node_modules\nCOPY . .\nRUN addgroup -g 1001 appgroup && \\\n    adduser -u 1001 -G appgroup -s /bin/sh -D appuser\nUSER appuser\nEXPOSE 3000\nHEALTHCHECK CMD wget -qO- http://localhost:3000/health || exit 1\nCMD ["node", "server.js"]', explanation: "Production-ready Dockerfile with multi-stage build." }, { language: "yaml", code: '# docker-compose.yml\nversion: "3.8"\nservices:\n  app:\n    build: .\n    ports: ["3000:3000"]\n    environment:\n      - NODE_ENV=production\n      - DATABASE_URL=mongodb://mongo:27017/myapp\n    depends_on: [mongo, redis]\n    restart: unless-stopped\n  mongo:\n    image: mongo:7\n    volumes: [mongo_data:/data/db]\n    ports: ["27017:27017"]\n  redis:\n    image: redis:7-alpine\n    ports: ["6379:6379"]\nvolumes:\n  mongo_data:', explanation: "Docker Compose for multi-service application." }, { language: "bash", code: '# Docker commands\ndocker build -t myapp:1.0 .\ndocker run -d -p 3000:3000 --name myapp myapp:1.0\ndocker logs -f myapp\ndocker exec -it myapp sh\n\n# Docker Compose\ndocker compose up -d\ndocker compose logs -f\ndocker compose down\ndocker compose down -v  # also remove volumes', explanation: "Essential Docker commands." }], exercises: [{ question: "Create a Dockerfile and docker-compose.yml for a Node.js app with MongoDB.", solution: '# Dockerfile\nFROM node:18-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nEXPOSE 3000\nCMD ["node", "server.js"]\n\n# docker-compose.yml\nservices:\n  app:\n    build: .\n    ports: ["3000:3000"]\n    environment: [MONGODB_URI=mongodb://mongo:27017/app]\n    depends_on: [mongo]\n  mongo:\n    image: mongo:7\n    volumes: [data:/data/db]\nvolumes:\n  data:', difficulty: "intermediate" }], externalLinks: [{ title: "Docker Docs", url: "https://docs.docker.com/" }], estimatedMinutes: 30 },
    { id: "dep-6", title: "Nginx & Reverse Proxy", content: "Nginx is a high-performance web server commonly used as a reverse proxy, load balancer, and static file server. A reverse proxy sits in front of your application servers, handling SSL termination, caching, compression, and routing. Nginx configuration uses a block-based syntax with server, location, and upstream directives.", codeExamples: [{ language: "nginx", code: '# /etc/nginx/sites-available/myapp\nserver {\n    listen 80;\n    server_name myapp.com www.myapp.com;\n    return 301 https://$host$request_uri;\n}\n\nserver {\n    listen 443 ssl http2;\n    server_name myapp.com;\n    \n    ssl_certificate /etc/letsencrypt/live/myapp.com/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/myapp.com/privkey.pem;\n    \n    # Serve React static files\n    root /var/www/myapp/dist;\n    index index.html;\n    \n    location / {\n        try_files $uri $uri/ /index.html;\n    }\n    \n    # Proxy API requests to backend\n    location /api/ {\n        proxy_pass http://localhost:3000;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n    }\n}', explanation: "Nginx as reverse proxy for React + API." }, { language: "nginx", code: '# Load balancing across multiple servers\nupstream backend {\n    least_conn;  # or round-robin, ip_hash\n    server 127.0.0.1:3001;\n    server 127.0.0.1:3002;\n    server 127.0.0.1:3003;\n}\n\nserver {\n    listen 443 ssl;\n    location /api/ {\n        proxy_pass http://backend;\n    }\n}\n\n# Caching\nlocation /static/ {\n    expires 1y;\n    add_header Cache-Control "public, immutable";\n}\n\n# Gzip compression\ngzip on;\ngzip_types text/plain text/css application/json application/javascript;\ngzip_min_length 1000;', explanation: "Load balancing, caching, and compression." }, { language: "bash", code: '# Nginx commands\nsudo nginx -t                    # test configuration\nsudo systemctl reload nginx      # apply changes\nsudo systemctl status nginx      # check status\n\n# Enable site\nsudo ln -s /etc/nginx/sites-available/myapp /etc/nginx/sites-enabled/\nsudo nginx -t && sudo systemctl reload nginx\n\n# View logs\nsudo tail -f /var/log/nginx/access.log\nsudo tail -f /var/log/nginx/error.log', explanation: "Nginx administration commands." }], exercises: [{ question: "Configure Nginx as a reverse proxy for a Node.js app with SSL and caching.", solution: 'server {\n    listen 443 ssl;\n    server_name myapp.com;\n    ssl_certificate /etc/letsencrypt/live/myapp.com/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/myapp.com/privkey.pem;\n    \n    location / {\n        root /var/www/myapp/dist;\n        try_files $uri /index.html;\n        expires 1h;\n    }\n    location /api/ {\n        proxy_pass http://localhost:3000;\n        proxy_set_header Host $host;\n    }\n}', difficulty: "intermediate" }], externalLinks: [{ title: "Nginx Docs", url: "https://nginx.org/en/docs/" }], estimatedMinutes: 25 },
    { id: "dep-7", title: "Environment Management", content: "Production applications need different configurations for development, staging, and production environments. Environment management involves managing secrets, feature flags, configuration files, and environment-specific build processes. Never hardcode secrets — use environment variables, secret managers, and .env files with proper gitignore rules.", codeExamples: [{ language: "javascript", code: '// Environment-specific configuration\nconst config = {\n  development: {\n    apiUrl: "http://localhost:3000",\n    debug: true,\n    dbUrl: "mongodb://localhost:27017/dev"\n  },\n  staging: {\n    apiUrl: "https://staging-api.myapp.com",\n    debug: true,\n    dbUrl: process.env.STAGING_DB_URL\n  },\n  production: {\n    apiUrl: "https://api.myapp.com",\n    debug: false,\n    dbUrl: process.env.DATABASE_URL\n  }\n};\n\nconst env = process.env.NODE_ENV || "development";\nmodule.exports = config[env];', explanation: "Environment-specific configuration." }, { language: "bash", code: '# .env files for different environments\n# .env.development\nVITE_API_URL=http://localhost:3000\nVITE_DEBUG=true\n\n# .env.production\nVITE_API_URL=https://api.myapp.com\nVITE_DEBUG=false\n\n# .env.local (never committed, overrides others)\nVITE_SECRET_KEY=my-local-secret\n\n# Vite loads env files in order:\n# .env                 (always loaded)\n# .env.local           (always loaded, gitignored)\n# .env.[mode]          (loaded for specific mode)\n# .env.[mode].local    (loaded for specific mode, gitignored)', explanation: "Vite environment file hierarchy." }, { language: "javascript", code: '// Feature flags\nconst features = {\n  newCheckout: process.env.FEATURE_NEW_CHECKOUT === "true",\n  darkMode: process.env.FEATURE_DARK_MODE === "true",\n  betaApi: process.env.FEATURE_BETA_API === "true"\n};\n\n// Usage in code\nif (features.newCheckout) {\n  return <NewCheckoutFlow />;\n}\nreturn <LegacyCheckout />;\n\n// Validate required env vars at startup\nconst required = ["DATABASE_URL", "JWT_SECRET", "SMTP_HOST"];\nfor (const key of required) {\n  if (!process.env[key]) {\n    console.error(`Missing required env var: ${key}`);\n    process.exit(1);\n  }\n}', explanation: "Feature flags and environment validation." }], exercises: [{ question: "Set up environment management with dev, staging, and production configs.", solution: '// 1. Create .env files: .env.development, .env.staging, .env.production\n// 2. Add to .gitignore: .env.local, .env.*.local\n// 3. Create .env.example with all keys (no values)\n// 4. Validate required vars at startup\n// 5. Use config module to load correct values\n// 6. Set NODE_ENV in deployment platform', difficulty: "intermediate" }], externalLinks: [{ title: "12-Factor App Config", url: "https://12factor.net/config" }], estimatedMinutes: 20 },
  ],
  hard: [
    { id: "dep-8", title: "CI/CD Pipelines", content: "Continuous Integration automatically tests code on every push. Continuous Deployment automatically deploys passing code to production. CI/CD pipelines catch bugs early, enforce code quality, and enable rapid, reliable releases. Popular tools include GitHub Actions, GitLab CI, and Jenkins.", codeExamples: [{ language: "yaml", code: '# .github/workflows/deploy.yml\nname: CI/CD Pipeline\non:\n  push:\n    branches: [main]\n  pull_request:\n    branches: [main]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: 18, cache: "npm" }\n      - run: npm ci\n      - run: npm run lint\n      - run: npm test\n      - run: npm run build\n\n  deploy:\n    needs: test\n    if: github.ref == \'refs/heads/main\'\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: amondnet/vercel-action@v25\n        with:\n          vercel-token: ${{ secrets.VERCEL_TOKEN }}\n          vercel-org-id: ${{ secrets.ORG_ID }}\n          vercel-project-id: ${{ secrets.PROJECT_ID }}\n          vercel-args: "--prod"', explanation: "GitHub Actions CI/CD with test and deploy." }, { language: "yaml", code: '# Docker-based pipeline\njobs:\n  build-and-push:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: docker/login-action@v3\n        with:\n          username: ${{ secrets.DOCKER_USERNAME }}\n          password: ${{ secrets.DOCKER_TOKEN }}\n      - uses: docker/build-push-action@v5\n        with:\n          push: true\n          tags: myapp:latest,myapp:${{ github.sha }}\n\n  deploy:\n    needs: build-and-push\n    runs-on: ubuntu-latest\n    steps:\n      - uses: appleboy/ssh-action@v1\n        with:\n          host: ${{ secrets.SERVER_HOST }}\n          username: ${{ secrets.SERVER_USER }}\n          key: ${{ secrets.SSH_KEY }}\n          script: docker pull myapp:latest && docker compose up -d', explanation: "Docker-based CI/CD with SSH deployment." }, { language: "yaml", code: '# Staging + production environments\ndeploy-staging:\n  needs: test\n  if: github.ref == \'refs/heads/develop\'\n  environment: staging\n  runs-on: ubuntu-latest\n  steps:\n    - run: echo "Deploy to staging"\n\ndeploy-production:\n  needs: test\n  if: github.ref == \'refs/heads/main\'\n  environment:\n    name: production\n    url: https://myapp.com\n  runs-on: ubuntu-latest\n  steps:\n    - run: echo "Deploy to production"', explanation: "Environment-based deployments." }], exercises: [{ question: "Set up a CI/CD pipeline with linting, testing, building, and deploying.", solution: '# .github/workflows/pipeline.yml\nname: Pipeline\non: { push: { branches: [main] } }\njobs:\n  ci:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: 18 }\n      - run: npm ci\n      - run: npm run lint\n      - run: npm test\n      - run: npm run build\n  cd:\n    needs: ci\n    runs-on: ubuntu-latest\n    steps:\n      - run: echo "Deploy"', difficulty: "advanced" }], externalLinks: [{ title: "GitHub Actions", url: "https://docs.github.com/en/actions" }], estimatedMinutes: 35 },
    { id: "dep-9", title: "Monitoring & Logging", content: "Production applications need monitoring for uptime, performance, and error tracking. Logging captures events for debugging. APM tools track response times, error rates, and resource usage. Alerting notifies teams when metrics exceed thresholds. Tools include Datadog, Sentry, and Grafana.", codeExamples: [{ language: "javascript", code: '// Structured logging with Winston\nconst winston = require("winston");\nconst logger = winston.createLogger({\n  level: process.env.LOG_LEVEL || "info",\n  format: winston.format.combine(\n    winston.format.timestamp(),\n    winston.format.json()\n  ),\n  transports: [\n    new winston.transports.Console(),\n    new winston.transports.File({ filename: "error.log", level: "error" }),\n    new winston.transports.File({ filename: "combined.log" })\n  ]\n});\n\nlogger.info("Server started", { port: 3000 });\nlogger.error("DB connection failed", { error: err.message });', explanation: "Structured logging with Winston." }, { language: "javascript", code: '// Error tracking with Sentry\nconst Sentry = require("@sentry/node");\nSentry.init({\n  dsn: process.env.SENTRY_DSN,\n  environment: process.env.NODE_ENV,\n  tracesSampleRate: 0.1\n});\n\napp.use(Sentry.Handlers.requestHandler());\n// Routes...\napp.use(Sentry.Handlers.errorHandler());\n\ntry { riskyOperation(); }\ncatch (err) {\n  Sentry.captureException(err);\n  logger.error("Operation failed", { error: err.message });\n}', explanation: "Error tracking with Sentry." }, { language: "javascript", code: '// Custom metrics endpoint\nlet requestCount = 0, errorCount = 0;\napp.use((req, res, next) => {\n  requestCount++;\n  const start = Date.now();\n  res.on("finish", () => {\n    if (res.statusCode >= 400) errorCount++;\n    logger.info("Request", { method: req.method, path: req.path, status: res.statusCode, duration: Date.now() - start });\n  });\n  next();\n});\n\napp.get("/metrics", (req, res) => {\n  res.json({ uptime: process.uptime(), requests: requestCount, errors: errorCount, memory: process.memoryUsage() });\n});', explanation: "Custom metrics and health check." }], exercises: [{ question: "Set up monitoring with structured logging, error tracking, and a health check.", solution: '// 1. Winston for structured logs\n// 2. Sentry for error tracking\n// 3. Health check: GET /health → { status: "ok", uptime }\n// 4. Metrics: GET /metrics → { requests, errors, memory }\n// 5. Alerting: configure Sentry alerts for error spikes', difficulty: "advanced" }], externalLinks: [{ title: "Sentry Docs", url: "https://docs.sentry.io/" }], estimatedMinutes: 30 },
    { id: "dep-10", title: "Kubernetes & Container Orchestration", content: "Kubernetes (K8s) automates deployment, scaling, and management of containerized applications. It groups containers into pods, manages them with deployments, exposes them via services, and handles configuration with ConfigMaps and Secrets. For most small-to-medium apps, managed platforms like Railway or Fly.io are simpler alternatives, but K8s is essential for large-scale production systems.", codeExamples: [{ language: "yaml", code: '# deployment.yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: myapp\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: myapp\n  template:\n    metadata:\n      labels:\n        app: myapp\n    spec:\n      containers:\n        - name: myapp\n          image: myapp:latest\n          ports:\n            - containerPort: 3000\n          env:\n            - name: NODE_ENV\n              value: production\n            - name: DATABASE_URL\n              valueFrom:\n                secretKeyRef:\n                  name: db-secret\n                  key: url\n          resources:\n            limits: { memory: "256Mi", cpu: "500m" }\n            requests: { memory: "128Mi", cpu: "250m" }\n          readinessProbe:\n            httpGet: { path: /health, port: 3000 }\n            initialDelaySeconds: 5', explanation: "Kubernetes Deployment with health checks and resource limits." }, { language: "yaml", code: '# service.yaml — expose the deployment\napiVersion: v1\nkind: Service\nmetadata:\n  name: myapp-service\nspec:\n  type: LoadBalancer\n  ports:\n    - port: 80\n      targetPort: 3000\n  selector:\n    app: myapp\n\n---\n# Horizontal Pod Autoscaler\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: myapp-hpa\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: myapp\n  minReplicas: 2\n  maxReplicas: 10\n  metrics:\n    - type: Resource\n      resource:\n        name: cpu\n        target: { type: Utilization, averageUtilization: 70 }', explanation: "Service and auto-scaling configuration." }, { language: "bash", code: '# kubectl commands\nkubectl apply -f deployment.yaml     # deploy\nkubectl get pods                      # list pods\nkubectl get services                  # list services\nkubectl logs -f pod-name              # view logs\nkubectl exec -it pod-name -- sh       # enter pod\nkubectl scale deployment myapp --replicas=5\nkubectl rollout status deployment myapp\nkubectl rollout undo deployment myapp  # rollback', explanation: "Essential kubectl commands." }], exercises: [{ question: "Create Kubernetes manifests for a web app with 3 replicas, a service, and auto-scaling.", solution: '# Apply all manifests\nkubectl apply -f deployment.yaml\nkubectl apply -f service.yaml\nkubectl apply -f hpa.yaml\n\n# Verify\nkubectl get pods  # should show 3 running\nkubectl get svc   # should show LoadBalancer\nkubectl get hpa   # should show min 2, max 10', difficulty: "advanced" }], externalLinks: [{ title: "Kubernetes Docs", url: "https://kubernetes.io/docs/" }], estimatedMinutes: 35 },
  ],
};
